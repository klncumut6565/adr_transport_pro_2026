# ADR Transport Pro 2026 - Masaüstü Bağımlılıkları
# Python 3.10+ önerilir

# Arayüz (ZORUNLU — bu dosyada eksikti, eklendi)
PyQt6>=6.6.0

# PDF Export (ReportLab)
reportlab>=3.6.0

# Excel Export/İçe Aktarma (openpyxl)
openpyxl>=3.0.0

# Karışık Yükleme motoru (adr_mix_pro) için
qrcode
Pillow

# Standart kütüphane modülleri (ekstra kurulum gerekmez):
#   tkinter (bazı dağıtımlarda ayrı paket: python3-tk), json, os, sys,
#   logging, datetime, threading, re, typing, ctypes, sqlite3
